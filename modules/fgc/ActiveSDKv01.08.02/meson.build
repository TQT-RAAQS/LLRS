activeSDK_inc = include_directories('include')
activeSDK_lib = meson.current_source_dir() / 'lib' / '*'
activeSDK_dep = declare_dependency(link_args: activeSDK_lib, include_directories: activeSDK_inc)
