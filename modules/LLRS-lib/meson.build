project('LLRS-lib', 
        'cuda', 'cpp', 'c'
        default_options: ['cpp_std=c++14', 'default_library=shared'],
        subproject_dir: 'modules')


# common-include (proproc.h and settings.h to be included in all modules)
llrs_lib_common_inc = include_directories('common-include')


