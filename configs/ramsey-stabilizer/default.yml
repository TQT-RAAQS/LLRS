memory_handler_config: default.yml
memory_handler_wait_s: 1
flag_configs_translator: false

fourier_analyzer:
  spatial_zero_padding_x: 128
  spatial_zero_padding_y: 128

saver_config:
  image_folder_name: raw_data
  delay_time_us: 50

pid_count: 5
pid_config:
  max_buffer_size: 10
  reload_needed: true

awg_handler:
  config: "default.yml"

qdac_client:
  active: true
  config: "default.yml"